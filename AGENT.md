# Agent Instructions

Use repository-scoped AI context only for this project.

## Required Workflow
1. Run `bash .codex/skills/odoo-platform-standards/scripts/specs_context.sh`.
2. Read all files listed under `Read now`.
3. Follow ADR guardrails before implementing changes.

## Local Context
- Canonical instructions file: `AGENT.md`
- Claude compatibility alias: `CLAUDE.md` (symlink)
- Agent local directory: `.codex` (aliased by `.claude`)
