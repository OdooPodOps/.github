# Local Codex Context
This repo uses project-scoped Codex skills to avoid global skill bleed.
Run Codex with local scope: `CODEX_HOME=$PWD/.codex`.
Refresh skill from source repo: `../odoo-ai-skills/scripts/install-project-skill.sh "$PWD"`.
